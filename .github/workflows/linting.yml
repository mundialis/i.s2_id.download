---
name: Linting and code quality check

on:
  push:
    branches:
      - main
      - develop
  pull_request:

jobs:
  lint:
    uses: mundialis/github-workflows/.github/workflows/linting.yml@main
    with:
      SUPER_LINTER_FILTER_REGEX_EXCLUDE: ".*processing/templates/template_S2_processing.json"
